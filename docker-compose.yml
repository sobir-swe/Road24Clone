services:
    road_web:
        container_name: road_web
        image: nginx:alpine  # Use the Nginx image
        ports:
            - "8000:80"  # Map port 8080 on the host to port 80 inside the container
        volumes:
            - ./:/var/www  # Mount the Laravel application directory
            - ./nginx.conf:/etc/nginx/conf.d/default.conf  # Use our Nginx config
        depends_on:
            - road_app  # Ensure PHP-FPM is started before Nginx
        networks:
            - road-network  # Connect to the custom network

    road_app:
        container_name: road_app
        build: .  # Build from the Dockerfile
        depends_on:
            - road_db
        volumes:
            - ./:/var/www  # Mount the Laravel application directory
        networks:
            - road-network  # Connect to the custom network

    road_db:
        container_name: road_db
        image: mysql:8  # Use MySQL image
        restart: always
        ports:
            - "3309:3306"
        environment:
            MYSQL_DATABASE: ${DB_DATABASE}  # Create a database named 'laravel'
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}  # Set the root password for MySQL
        volumes:
            - road_db:/var/lib/mysql  # Persist MySQL data
        networks:
            - road-network  # Connect to the custom network

    road_node:
        container_name: road_node
        image: node:20
        working_dir: /var/www
        volumes:
            - ./:/var/www
        networks:
            - road-network
        command: bash -c "npm install && npm run build"
# Define the custom network
networks:
    road-network:  # Create a custom network named 'online_store-network'
        driver: bridge

# Define the volume for MySQL data persistence
volumes:
    road_db:
